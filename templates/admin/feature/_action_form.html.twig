<form action="{{ url('admin_feature_delete', {id: feature.id}) }}" method="post" data-confirmation="true"
      id="delete-form-{{ feature.id }}">

    <a href="{{ url('admin_feature_edit', {id: feature.id}) }}"
       class="btn btn-sm btn-outline-primary">{{ 'action.edit' | trans }}</a> &nbsp;&nbsp;&nbsp;

    <input type="hidden" name="token" value="{{ csrf_token('delete') }}"/>
    <button type="submit" class="btn btn-sm btn-danger"
            onclick="if(confirm('{{ 'confirmation.delete_item' | trans }}')){ return true }else{ return false }">
        {{ 'action.delete' | trans }}
    </button>
</form>
