{% extends 'admin/base.html.twig' %}

{% block title %}{{ 'title.homepage_settings' | trans }}{% endblock %}

{% block body %}

    <div class="card mb-3">
        <div class="card-header"><i class="fas fa-edit"></i> {{ 'title.homepage_settings' | trans }}</div>
        <div class="card-body">

            {{ form_start(form) }}

            <div class="form-group">

                {{ form_row(form.homepage_title) }}

            </div>

            <div class="form-group">
                {{ form_row(form.homepage_description) }}
            </div>

            <div class="form-group">
                {{ form_row(form.homepage_meta_tags) }}
            </div>

            <div class="form-group">

                <button type="submit" class="btn btn-primary">
                    {{ 'action.save'|trans }}
                </button>

            </div>

            {{ form_end(form) }}

        </div>
    </div><br>

{% endblock %}
