{{ form_start(form) }}

<div class="row">
    <div class="col-lg-6 col-12 mb-3">
        {{ form_row(form.show_filter_by_city) }}
    </div>
    <div class="col-lg-6 col-12 mb-3">
        {{ form_row(form.show_filter_by_deal_type) }}
    </div>
</div>

<div class="row">
    <div class="col-lg-6 col-12 mb-3">
        {{ form_row(form.show_filter_by_category) }}
    </div>
    <div class="col-lg-6 col-12 mb-3">
        {{ form_row(form.show_filter_by_bedrooms) }}
    </div>
</div>

<div class="form-group">
    <button type="submit" class="btn btn-primary">
        {{ 'action.save'|trans }}
    </button>
</div>

{{ form_end(form) }}
