<form action="{{ url('admin_menu_delete', {id: item.id}) }}" method="post" data-confirmation="true"
      id="delete-form-{{ item.id }}">

    <a href="{{ url('admin_menu_edit', {id: item.id}) }}"
       class="btn btn-sm btn-outline-dark">{{ 'action.edit' | trans }}</a> &nbsp;&nbsp;&nbsp;

    <input type="hidden" name="token" value="{{ csrf_token('delete') }}"/>
    <button type="submit" class="btn btn-sm btn-danger"
            onclick="if(confirm('{{ 'confirmation.delete_item' | trans }}')){ return true }else{ return false }">
        {{ 'action.delete' | trans }}
    </button>
</form>
