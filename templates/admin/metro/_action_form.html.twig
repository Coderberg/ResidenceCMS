<form action="{{ url('admin_metro_delete', {id: station.id}) }}" method="post" data-confirmation="true"
      id="delete-metro-{{ station.id }}">

    <a href="{{ url('admin_metro_edit', {id: station.id}) }}"
       class="btn btn-sm btn-outline-primary">{{ 'action.edit' | trans }}</a> &nbsp;&nbsp;&nbsp;

    <input type="hidden" name="token" value="{{ csrf_token('delete') }}"/>
    <button type="submit" class="btn btn-sm btn-danger"
            onclick="if(confirm('{{ 'confirmation.delete_item' | trans }}')){ return true }else{ return false }">
        {{ 'action.delete' | trans }}
    </button>
</form>
