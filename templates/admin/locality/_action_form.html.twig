<form action="{{ url('admin_locality_delete', {id: locality.id}) }}" method="post" data-confirmation="true"
      id="delete-form-{{ locality.id }}">

    <a href="{{ url('admin_locality_edit', {id: locality.id}) }}"
       class="btn btn-outline-primary">{{ 'action.edit' | trans }}</a> &nbsp;&nbsp;&nbsp;

    <input type="hidden" name="token" value="{{ csrf_token('delete') }}"/>
    <button type="submit" class="btn btn-danger"
            onclick="if(confirm('{{ 'confirmation.delete_item' | trans }}')){ return true }else{ return false }">
        {{ 'action.delete' | trans }}
    </button>
</form>
