<form action="{{ url('admin_neighborhood_delete', {id: neighborhood.id}) }}" method="post"
      id="delete-neighborhood-{{ neighborhood.id }}">

    <input type="hidden" name="token" value="{{ csrf_token('delete') }}"/>

    <a href="{{ url('admin_neighborhood_edit', {id: neighborhood.id}) }}"
       class="btn btn-sm btn-outline-primary">{{ 'action.edit' | trans }}</a> &nbsp;&nbsp;&nbsp;

    {{ include('common/_delete_button.html.twig') }}

</form>
